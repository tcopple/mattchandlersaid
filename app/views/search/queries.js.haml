:plain
  $('#info').slideUp('fast', function() {
    $(this).html('#{escape_javascript(render :partial => "search/option_queries", :locals => { :list => @queries })}');
    $(this).slideDown();
  });
